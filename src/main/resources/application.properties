# ═══════════════════════════════════════════════════════════════
# Jewelry Management System – Application Configuration
# ═══════════════════════════════════════════════════════════════

# ── Database ─────────────────────────────────────────────────────
# Update password to match your MySQL installation
db.url=jdbc:mysql://localhost:3306/jewelry_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=password

# ── HikariCP Connection Pool ──────────────────────────────────────
db.pool.maximumPoolSize=10
db.pool.minimumIdle=2
db.pool.connectionTimeout=30000

# ── Application ───────────────────────────────────────────────────
app.name=Jewelry Management System
app.version=1.0.0
